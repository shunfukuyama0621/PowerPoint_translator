# LibreTranslate のエンドポイントURL
# docker-compose 利用時はデフォルト値のままでOK（同一ネットワーク内で解決される）
# 別ホストのLibreTranslateを指す場合は http://<ホストIP>:5000 に変更
LIBRETRANSLATE_URL=http://libretranslate:5000

# LibreTranslate を APIキー保護モードで起動した場合のみ設定（通常は空欄でOK）
LIBRETRANSLATE_API_KEY=
